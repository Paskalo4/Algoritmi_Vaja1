#ifndef SORTING_HPP
#define SORTING_HPP

#include <vector>

bool Branje_Stevil(std::vector<int>& vec, const char s[]);
void Izpis_Stevil(int* polje, unsigned int velikost);
void Counting_Sort(std::vector<int>& A);
void Roman_Sort(std::vector<int>& A);

#endif
